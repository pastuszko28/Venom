# VENOM default install profile (minimal API / cloud-first)
#
# This default intentionally does NOT install local heavy LLM engines.
# It gives a lightweight start and lets user choose runtime profile later.
#
# Profiles:
# - API only:      pip install -r requirements.txt
# - Ollama local:  pip install -r requirements-profile-ollama.txt
# - vLLM local:    pip install -r requirements-profile-vllm.txt
# - ONNX LLM:      pip install -r requirements-profile-onnx.txt
# - ONNX CPU-only: pip install -r requirements-profile-onnx-cpu.txt
# - ONNX extras:   pip install -r requirements-extras-onnx.txt  (faster-whisper + piper-tts)
# - Full legacy:   pip install -r requirements-full.txt

-r requirements-profile-api.txt
